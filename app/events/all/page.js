import {getEvents} from '../../../lib/data';import AllEventsGrid from '../../../components/AllEventsGrid';export default async function AllEventsPage(){const events=await getEvents();return <div className="page-wrap"><header className="page-head"><div className="eyebrow">Browse the full calendar</div><h1>All Events</h1><p>Filter upcoming engineering and campus events by the group responsible for them.</p></header><AllEventsGrid events={events}/></div>}