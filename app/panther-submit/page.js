import SubmissionGate from '../../components/SubmissionGate';

export const metadata = { title: 'Panther Submit' };

export default async function PantherSubmitPage() {
  return <SubmissionGate />;
}
