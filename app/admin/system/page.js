import { getViewer, isSuperAdmin } from '../../../lib/auth';
export default async function SystemInsightsPage() { const viewer = await getViewer(); if (!viewer.user || !isSuperAdmin(viewer)) return <div className="workspace-auth-card"><h1>Super Admin access required</h1><p>System Insights is restricted to the Super Admin role.</p></div>; return <div className="workspace-placeholder"><div className="eyebrow">Super Admin operations</div><h1>System Insights</h1><p>Parser, OCR, upload, notification, and bug-report observability are not yet persisted by the current provider. No health or accuracy numbers are fabricated here.</p><p className="workspace-status">Next pass: connect existing intake evidence and failure records to an auditable operational view.</p></div>; }
